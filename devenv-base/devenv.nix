{ pkgs, ... }: {
  # Scommenta il linguaggio che ti serve
  # languages.python.enable = true;
  # languages.rust.enable = true;
  # languages.go.enable = true;

  packages = [
    # Aggiungi qui gli LSP (pyright, rust-analyzer, gopls)
  ];
}
